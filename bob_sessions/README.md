# IBM Bob IDE Session Evidence

Every material Bob IDE task used for this submission must have
its task/session consumption summary captured here as a PNG.

Naming convention:

taskNN_short_description_summary.png

After capturing each screenshot, register it using:

.\scripts\Add-BobSessionEvidence.ps1
